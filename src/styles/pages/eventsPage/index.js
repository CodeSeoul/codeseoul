import styled from 'styled-components';
import Header from './header';
const EventsPageWrapper = styled.div`
  box-sizing: border-box;
  section {
    text-align: center;
  }
`;

EventsPageWrapper.Header = Header;

export default EventsPageWrapper;
